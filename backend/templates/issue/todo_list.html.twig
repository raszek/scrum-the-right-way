<div class="card mb-3">
    <div class="card-header d-flex justify-content-between align-items-center">
        To do list
    </div>
    <div class="card-body">
        {{ render(path('widget_todo_list', {
            state: {items: []} | json_encode()
        })) }}
    </div>
</div>
