<div class="row">
    <div class="col">
        {{ form_start(form) }}
        {{ form_row(form.email) }}
        {{ form_row(form.firstName) }}
        {{ form_row(form.lastName) }}

        <button class="btn btn-primary">{{ buttonSubmit }}</button>
        {{ form_end(form) }}
    </div>
</div>
