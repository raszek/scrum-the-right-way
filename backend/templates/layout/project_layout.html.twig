{% extends 'base.html.twig' %}

{% block body %}

    {{ render(path('app_menu', {currentPath: app.request.pathinfo})) }}

    <div class="container-fluid">
        <div class="row">
            <div class="sidebar col-md-2 p-3 bg-body-tertiary">
                {{ render(path('app_project_sidebar', {
                    id: project.id,
                    currentPath: app.request.pathinfo
                })) }}
            </div>
            <main class="col-md-10">
                {% include 'common/alert.html.twig' %}

                {% block content %}{% endblock %}
            </main>
        </div>

    </div>
{% endblock %}
